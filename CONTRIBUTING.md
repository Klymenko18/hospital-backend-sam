# Contributing

## Development
- Python 3.11+.
- Keep code clean and documented with **docstrings** (minimal inline comments).
- Run locally: `sam build && sam local start-api`.

## Quality
- Install pre-commit hooks:  
  ```bash
  pip install pre-commit && pre-commit install
